{
  "name": "far2048",
  "version": "1.0.0",
  "description": "4-player real-time 2048 battle game on Farcaster with USDC betting",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "contracts"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "deploy:contracts": "cd contracts && npx hardhat run scripts/deploy.js",
    "setup": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../contracts && npm install"
  },
  "keywords": [
    "farcaster",
    "mini-app",
    "2048",
    "web3",
    "multiplayer",
    "betting"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

